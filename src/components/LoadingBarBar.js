import React from 'react';
import LoadingBar from 'react-redux-loading-bar';

function LoadingBarBar() {
  return (
    <div className="loadingBar">
      <LoadingBar />
    </div>
  );
}

export default LoadingBarBar;
